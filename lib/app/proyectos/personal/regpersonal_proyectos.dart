import 'package:flutter/material.dart';

class regpersonal_proyectos extends StatelessWidget {
  const regpersonal_proyectos({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Hola'),
      ),
    );
  }
}
